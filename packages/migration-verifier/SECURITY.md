# Security policy

Report vulnerabilities through GitHub private vulnerability reporting. This tool is offline, uses no runtime dependencies, bounds its primary collections, and returns explicit errors instead of guessing success. Never use real secrets, client data, private topology, or production records in examples and issues.

