[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="backup-verifier"
version="0.1.0"
description="Offline backup manifest integrity verifier"
readme="README.md"
requires-python=">=3.11"
license={text="Apache-2.0"}
dependencies=[]
[project.scripts]
backup-verifier="backup_verifier:main"
[tool.setuptools]
package-dir={""="src"}
[tool.setuptools.packages.find]
where=["src"]
